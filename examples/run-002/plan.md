# Phase 2 dogfood — independent pure helpers

Done when: pytest workspace tests green for slugify, clamp, parse_kv, merkle_join.
Cost intent: Muse free workers implement; manager only tests/briefs/gates.
